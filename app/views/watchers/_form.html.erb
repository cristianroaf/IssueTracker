<%= form_with(model: watcher, local: true) do |form| %>
  <% if watcher.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(watcher.errors.count, "error") %> prohibited this watcher from being saved:</h2>

      <ul>
        <% watcher.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :issue_id %>
    <%= form.text_field :issue_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
